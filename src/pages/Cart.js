import { useState, useEffect, useRef } from "react";

const Cart = () => {
  const [cart, setCart] = useState(null);
  return (
    <div>
      <h1>Your Cart</h1>
    </div>
  );
};

export default Cart;
