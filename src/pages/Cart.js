import { useState, useEffect, useRef } from "react";

const [cart, setCart] = useState(null);

const Cart = () => {
  return (
    <div>
      <h1>Your Cart</h1>
    </div>
  );
};

export default Cart;
